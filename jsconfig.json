{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["*"],             // @ artık proje kökü: lib, app, components hepsi bulunur
      "@app/*": ["app/*"],      // (opsiyonel) açık isimli alias
      "@lib/*": ["lib/*"]       // (opsiyonel) istersen kalsın
    }
  }
}
